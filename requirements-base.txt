# WildCatcher - Shared dependencies (all platforms)
#
# Inference runs on ONNX Runtime with GPU acceleration:
#   Windows -> DirectML (any DirectX 12 GPU), macOS -> CoreML, NVIDIA -> CUDA EP.
#
# PyTorch is needed ONLY to convert user-added .pt/.pth models to ONNX at import
# time (one-shot, in wc_convert). Install the small CPU-ONLY build — it is NOT
# used for inference, so the heavy CUDA build is unnecessary:
#   Windows/Linux: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   macOS:         pip install torch torchvision

# --- Inference runtime (GPU) ---
onnxruntime-directml>=1.17.0 ; sys_platform == "win32"
onnxruntime>=1.17.0 ; sys_platform != "win32"
# onnx is required by torch.onnx.export when converting user-added models
onnx>=1.15.0

# --- App framework / IO ---
opencv-python>=4.8.0
PyQt5>=5.15.0
Pillow>=9.0.0
pycryptodome>=3.18.0
python-vlc>=3.0.0
numpy>=1.24.0
openpyxl>=3.1.0
psutil>=5.9.0
requests>=2.28.0

# Removed (were only pulled in by the old yolov5/ultralytics detector path):
#   ultralytics, matplotlib, seaborn, pandas, scipy, tqdm, PyYAML
